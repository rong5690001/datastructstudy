//// Created by laura on 2020/3/29.//#include "MGraph.h"#include "iostream"using namespace std;void CreateMGraph(MGraph *mGraph) {    cout << "请输入图的顶点数：";    cin >> mGraph->numVertexes;    cout << "请输入图的边数：";    cin >> mGraph->numEdges;    for (int i = 0; i < mGraph->numVertexes; ++i) {        cout << "请输入图的各个顶点的值：";        cin >> mGraph->vexs[i];    }    for (int i = 0; i < mGraph->numVertexes; ++i) {        for (int j = 0; j < mGraph->numVertexes; ++j) {            mGraph->arc[i][j] = INFINITY;        }    }    for (int k = 0; k < mGraph->numEdges; ++k) {        cout << "请输入(vi,vj)的下标和对应边上的权：";        int i, j, w;        cin >> i;        cin >> j;        cin >> w;        mGraph->arc[i][j] = w;        //因为是无向图，所以是对称矩阵        mGraph->arc[j][i] = w;    }    for (int l = 0; l < mGraph->numVertexes; ++l) {        for (int i = 0; i < mGraph->numVertexes; ++i) {            cout << mGraph->arc[l][i]<< "    ";        }        cout << endl;    }}void MGraph_test() {    MGraph mGraph = MGraph();    CreateMGraph(&mGraph);}